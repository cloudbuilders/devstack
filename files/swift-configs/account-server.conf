[DEFAULT]
devices = /srv/NODENUM/node
mount_check = false
bind_port = 60NODENUM2
user = USERNAME
log_facility = LOG_LOCALLOGNUM

[pipeline:main]
pipeline = account-server

[app:account-server]
use = egg:swift#account

[account-replicator]
vm_test_mode = yes

[account-auditor]

[account-reaper]
